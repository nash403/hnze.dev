{
  "name": "hnze-dev",
  "dependencies": {
    "@nuxt/content": "^3.4.0",
    "nuxt": "^3.16.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "clean:generated": "rm -rf .nuxt .data .output",
    "preinstall": "node -e \"if(process.env.CI == 'true') {console.log('Skipping preinstall...')} else {process.exit(1)}\" || npx -y only-allow bun",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:ssl": "WEBAPP_LOCAL_USE_SSL=1 CAROOT=\"$(mkcert -CAROOT)\" NODE_TLS_REJECT_UNAUTHORIZED=0 nuxi dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxi typecheck",
    "postinstall": "nuxt prepare"
  }
}
